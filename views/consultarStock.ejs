<!DOCTYPE html>
<html>
<head>
  <title>Consultar Stock</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1>Consultar Stock por Código</h1>

  <form action="/productos/consultar-stock" method="POST">
    <label for="codigo">Código del Producto:</label>
    <input type="text" name="codigo" id="codigo" required>
    <button type="submit">Consultar</button>
  </form>

  <% if (error) { %>
    <p style="color: red;"><%= error %></p>
  <% } %>

  <% if (producto) { %>
    <h2>Resultado</h2>
    <ul>
      <li><strong>Nombre:</strong> <%= producto.nombre %></li>
      <li><strong>Stock Actual:</strong> <%= producto.stockActual %></li>
      <li><strong>Stock Mínimo:</strong> <%= producto.stockMinimo %></li>
      <li><strong>Categoría:</strong> <%= producto.categoria %></li>
      <li><strong>Precio:</strong> $<%= producto.precio.toFixed(2) %></li>
    </ul>
  <% } %>

  <a href="/">← Volver al inicio</a>
</body>
</html>